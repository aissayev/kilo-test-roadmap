# Notion integration (optional for v1).
# When BOTH are set, the app fetches the roadmap from Notion; otherwise it falls
# back to the sample data in lib/roadmap-data.ts, so the build never fails.
NOTION_API_KEY=
NOTION_ROADMAP_DB_ID=
