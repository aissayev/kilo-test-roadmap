# Netlify configuration — Next.js static export.
# `next build` with output:'export' emits a static site into `out/`.
[build]
  command = "npm run build"
  publish = "out"

[build.environment]
  NODE_VERSION = "20"
